name: Cellblock CI

on:
  push:
    paths:
      - "cellblock/**"
      - ".github/workflows/cellblock-ci.yml"
  pull_request:
    paths:
      - "cellblock/**"
      - ".github/workflows/cellblock-ci.yml"

defaults:
  run:
    working-directory: cellblock

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ["18", "20"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install cellblock (editable, with dev extras)
        run: pip install -e ".[dev]"

      - name: Run pytest
        run: pytest

      - name: Run player-core tests
        run: node --test tests/player/*.test.mjs
